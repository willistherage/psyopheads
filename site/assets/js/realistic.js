
var lab;

$(function() {

	// Create reference to single animation frame globally.
	window.animationframe = new AnimationFrame();
	
	// The model for communication with the socket server.
	var lab = new Laboratory();
	
});